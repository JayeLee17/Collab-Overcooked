**Skill**: 
In this game, you can ONLY perform the following allowed actions. Do not attempt to use any other actions that are not listed here. If there are parameters (like steps, utensil), you need to fill them without using quotation marks, angle brackets, etc. There are 2 kinds of actions: 'operation actions' and 'requests'. If you want a teammate to perform a certain operation action, you need to generate a 'request' action with the operation action as an argument. 
**'Operation actions'**: 
def pickup(obj, place):
    if object_in_hand() == "nothing": 
       if place in utensil_list or place == "counter" or place == "dish_dispenser" or place ==“ingredient_dispenser":
            if object_in_place(obj, place):
                return

def cut(chopping_board_name):
    """ To start cutting in the chopping_board """
    if has_food(chopping_board_name):
        return

def place_obj_on_counter():
    if object_in_hand() != "nothing":
        return

def put_obj_in_utensil(utensil):
    if object_in_hand() != "nothing":
        if utensil in utensil_list:
            return

def fill_dish_with_food(utensil):
    if object_in_hand() == "dish":
        if utensil_food_ready() or utensil_started_cooking():
            return
 
    """ To start blender example: stir(blender0) """
def stir(blender_name):
    if has_food(blender_name):
        return

def wait(num):
    # wait positive num timesteps
    if isinstance(num, int) and 0 < num <= 20:
        return

**Strict utensil-action mapping (MUST follow):**
- `cut()` can ONLY target `chopping_board*`
- `stir()` can ONLY target `blender*`
- `cook()` can ONLY target `pot*` (Assistant normally should not output this unless map/role explicitly allows)
- `bake()` can ONLY target `oven*` (Assistant normally should not output this unless map/role explicitly allows)
- `wash()` can ONLY target `water*`
- Invalid example: `cook(chopping_board0)`, `cut(oven0)`, `stir(pot0)`

Before outputting `Action: ...`, run this check:
1) Is my action in allowed action list?  
2) If targeting a utensil, does verb match utensil type exactly?  
If not, replace with a valid action or `wait(1)`.
        
**'Cooperative action'**:  
Use `Collab(...)` to express cooperation within a timestep. Collaboration does not consume extra timesteps: once you emit a `Collab`, treat it as if the teammate instantly replied. A `Collab` can only contain **one** primitive type; only `request` may appear multiple times separated by semicolons. Examples:
- `Collab(seek(Chef, "What should I do？"))`
- `Collab(deny(Chef, "I cannot reach the oven"))`
- `Collab(ack(Chef))`

Available cooperative primitives:
```
def request(receiver,operation_action):
    send_message(content=operation_action, to=receiver)
    return

def seek(receiver, message):
    send_message(content=message, to=receiver)
    return

def deny(receiver, message):
    send_message(content=message, to=receiver)
    return

def ack(receiver, message):
    send_message(content=message, to=receiver)
    return
``` 
